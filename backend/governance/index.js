'use strict';const{createGovernedRouter}=require('./router');const{pool}=require('../db');const{authenticateToken:auth}=require('../middleware/auth');const{evaluate}=require('./domain');module.exports=createGovernedRouter({pool,auth,evaluate,workflowType:'genealogy-research',providers:['archive','records','ocr','maps','gedcom','document-storage','collaboration'],approverRoles:['researcher','reviewer','admin']});
